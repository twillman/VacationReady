<h2>Welcome, <%=@user.name %>!</h2>
<h4>You have <%= @user.vacations.count %> saved vacations.</h4>
<%= link_to "New Vacation", new_vacation_path, class: 'btn btn-success' %></br>
<%= @user.vacations.each do |vacation| %>
  <%= link_to vacation.name, vacation_path(vacation) %></br>
<% end %>
